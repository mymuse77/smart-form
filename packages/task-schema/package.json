{
  "name": "@smart-form/task-schema",
  "version": "0.1.0",
  "private": true,
  "description": "任务定义 Schema（含 mode: read|write）",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@smart-form/contracts": "workspace:*",
    "zod": "^3.25.0"
  }
}
