{
  "name": "@smart-form/event-schema",
  "version": "0.1.0",
  "private": true,
  "description": "事件 Schema 定义与校验",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@smart-form/contracts": "workspace:*",
    "zod": "^3.25.0"
  }
}
